# require 'rails_helper'
#
# RSpec.describe "posts/show.html.erb", type: :view do
#
#   # factory bot instead of seeds, equivalent to   Uesr.new({})
#   let(:post) { build(:user) }
#   # let!(:post) { build(:user) }  # let!  means it gets saved right away instead of only happenening in the tests that call it???
#
#   # let(:post) { build(:random_post, content: 'this is content I want') }
#   let(:post) { build(:random_post) }
#
#
#   # let(:post) { create(:user) }  # equivalent to User.create({})
#
#
#   context '....' do
#     let(:posts) { create_list(:random_post, 24) }
#     it '...' do
#       expect(Post.......size).to eq(num)
#     end
#   end
#
#
#
#
#
# end
