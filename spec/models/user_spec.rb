require 'rails_helper'

RSpec.describe User, type: :model do

  # before do
  #   Rails.application.load_seed
  # end
  #
  # # let(:user) { User.find_by(name: 'Person1') }
  #
  #
  # context "is not valid if password length is less than 8" do
  #   it 'password is too short' do
  #     user = User.new(password: 'passwor')
  #     user.validate
  #     expect(user.errors[:password]).to include('is too short (minimum is 8 characters)')
  #   end
  # end
  # # just to test if seed is working
  # # context "Username cannot be Ariel" do
  # #   it 'User picked the wrong name' do
  # #     expect(User.third.first_name).to eql('Ariel')
  # #   end
  # # end


end
